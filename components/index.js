import * as basicButtons from "./basicButtons.js";
import * as classButtons from "./classButtons.js";
import * as gpaButtons from "./gpaButtons.js";
import * as modalButtons from "./modalButtons.js";

export const components = {
    ...basicButtons,
    ...classButtons,
    ...gpaButtons,
};

export const modalSubmit = {
    ...modalButtons,
}